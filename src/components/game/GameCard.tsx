import React from 'react';

interface GameCardProps {
    // TODO: Add props
}

const GameCard: React.FC<GameCardProps> = () => {
    return (
        <div className="game-card">
            {/* TODO: Implement Half-Half UI */}
            GameCard Skeleton
        </div>
    );
};

export default GameCard;
